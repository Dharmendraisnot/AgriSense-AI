# ============================================================
#  AgriSense AI – Python Dependencies
#  Tested with Python 3.12 on Windows / Linux / macOS
#
#  Key constraint: ibm-watsonx-ai 1.1.x requires pandas<2.2.0
#  so we use pandas==2.1.4 (latest compatible release).
# ============================================================

# ── Web Framework & UI ──────────────────────────────────────
streamlit==1.35.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx>=0.27.0              # ibm-watsonx-ai pulls 0.28.x; allow >=0.27

# ── IBM watsonx.ai SDK ───────────────────────────────────────
ibm-watsonx-ai==1.1.26    # requires pandas<2.2.0

# ── PyTorch (CPU-only – no GPU/CUDA drivers required) ────────
# Install with: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.2               # CPU wheel; required by sentence-transformers

# ── RAG & Vector Store ───────────────────────────────────────
chromadb>=1.0.0            # 1.x ships pre-built Windows wheels (no C++ compiler needed)
sentence-transformers==3.0.1
langchain==0.2.5
langchain-community==0.2.5
langchain-ibm==0.1.7
pypdf==4.2.0
python-docx==1.1.2

# ── Data & ML ────────────────────────────────────────────────
pandas==2.1.4              # latest version satisfying ibm-watsonx-ai<2.2.0
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.4.2

# ── Configuration ────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv==1.0.1

# ── HTTP & APIs ──────────────────────────────────────────────
requests==2.32.3
aiohttp==3.9.5

# ── Utilities ────────────────────────────────────────────────
loguru==0.7.2
tenacity==8.3.0
python-multipart==0.0.9

# ── Testing ──────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
